name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/**'
      - '.github/workflows/deploy-supabase.yml'
  workflow_dispatch:  # Allow manual trigger

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate Secrets
        run: |
          echo "Validating required secrets..."
          if [ -z "${{ secrets.SUPABASE_ACCESS_TOKEN }}" ]; then
            echo "❌ SUPABASE_ACCESS_TOKEN secret is not set"
            echo "Please configure it in repository Settings → Secrets and variables → Actions"
            exit 1
          fi
          if [ -z "${{ secrets.SUPABASE_PROJECT_ID }}" ]; then
            echo "❌ SUPABASE_PROJECT_ID secret is not set"
            echo "Please configure it in repository Settings → Secrets and variables → Actions"
            exit 1
          fi
          echo "✅ All required secrets are configured"

      - name: Deploy Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        run: |
          set -e  # Exit on any error

          echo "🚀 Deploying Edge Functions to Supabase..."
          echo "Project: $SUPABASE_PROJECT_ID"
          echo ""

          # Link to project
          echo "📡 Linking to Supabase project..."
          supabase link --project-ref $SUPABASE_PROJECT_ID
          echo "✅ Successfully linked to project"
          echo ""

          # Deploy all Edge Functions
          echo "📦 Deploying health..."
          supabase functions deploy health --no-verify-jwt
          echo "✅ health deployed"
          echo ""

          echo "📦 Deploying invite-staff-member..."
          supabase functions deploy invite-staff-member --no-verify-jwt=false
          echo "✅ invite-staff-member deployed"
          echo ""

          echo "📦 Deploying reset-staff-password..."
          supabase functions deploy reset-staff-password --no-verify-jwt=false
          echo "✅ reset-staff-password deployed"
          echo ""

          echo "📦 Deploying kajabi-webhook..."
          supabase functions deploy kajabi-webhook --no-verify-jwt
          echo "✅ kajabi-webhook deployed"
          echo ""

          echo "📦 Deploying paypal-webhook..."
          supabase functions deploy paypal-webhook --no-verify-jwt
          echo "✅ paypal-webhook deployed"
          echo ""

          echo "📦 Deploying ticket-tailor-webhook..."
          supabase functions deploy ticket-tailor-webhook --no-verify-jwt
          echo "✅ ticket-tailor-webhook deployed"
          echo ""

          echo "🎉 All 6 edge functions deployed successfully!"

      - name: Verify Deployment
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        run: |
          echo "Verifying deployed functions..."
          supabase functions list

      - name: Notify on Success
        if: success()
        run: |
          echo "🎉 Deployment successful!"
          echo "Functions deployed:"
          echo "  - health (no JWT verification)"
          echo "  - invite-staff-member (JWT required)"
          echo "  - reset-staff-password (JWT required)"
          echo "  - kajabi-webhook (no JWT verification)"
          echo "  - paypal-webhook (no JWT verification)"
          echo "  - ticket-tailor-webhook (no JWT verification)"

      - name: Notify on Failure
        if: failure()
        run: |
          echo "❌ Deployment failed. Check logs above for details."
          exit 1
