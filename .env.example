# StarHouse Database V2 - Environment Configuration Template
# =============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values (NEVER use these examples in production!)
# 3. Verify .env is in .gitignore (it should be)
# 4. NEVER commit .env to git!
# =============================================================================

# =============================================================================
# DATABASE CONNECTION (REQUIRED)
# =============================================================================
# Get this from Supabase Dashboard:
# https://app.supabase.com/project/YOUR_PROJECT_ID/settings/database
#
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres.abc123:your_password_here@aws-0-region.pooler.supabase.com:6543/postgres
#
# SECURITY: This is the ONLY place credentials should exist
# - Scripts use secure_config.py to load this
# - No hardcoded defaults anywhere
# - Fails fast if not set
#
DATABASE_URL=postgresql://user:password@host:port/database

# =============================================================================
# IMPORT CONFIGURATION (Optional)
# =============================================================================
KAJABI_BATCH_SIZE=1000
LOG_LEVEL=INFO
ENVIRONMENT=development

# =============================================================================
# WEBHOOK SECRETS (Optional - only needed for webhook integrations)
# =============================================================================
# These verify webhook requests from external services
# Get these from each service's webhook configuration page
#
# KAJABI_WEBHOOK_SECRET=your_kajabi_secret_here
# PAYPAL_WEBHOOK_SECRET=your_paypal_secret_here
# TICKET_TAILOR_WEBHOOK_SECRET=your_ticket_tailor_secret_here

# =============================================================================
# API KEYS (Optional - only needed for API integrations)
# =============================================================================
# ZOHO_API_KEY=your_zoho_api_key_here
# USPS_API_KEY=your_usps_api_key_here

# =============================================================================
# MONITORING (Optional)
# =============================================================================
# DATADOG_API_KEY=your_datadog_api_key
# ENABLE_METRICS=false

# =============================================================================
# RETRY CONFIGURATION (Optional)
# =============================================================================
MAX_RETRIES=3
RETRY_BACKOFF_SECONDS=1

# =============================================================================
# SECURITY CHECKLIST
# =============================================================================
# ✅ .env file is in .gitignore
# ✅ Never commit .env to git
# ✅ Rotate credentials if exposed (see docs/CRITICAL_SECURITY_FIXES_CORRECTED.md)
# ✅ Use different credentials for dev/staging/production
# ✅ Use read-only credentials where possible
# ✅ Monitor for suspicious database access
# ✅ Keep credentials out of logs and error messages
#
# If credentials are exposed in git:
# 1. Rotate credentials IMMEDIATELY in Supabase
# 2. Purge git history (BFG Repo-Cleaner or git-filter-repo)
# 3. See: docs/CRITICAL_SECURITY_FIXES_CORRECTED.md
#
